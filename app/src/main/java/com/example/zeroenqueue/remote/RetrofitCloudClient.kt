package com.example.zeroenqueue.remote

import retrofit2.Retrofit

object RetrofitCloudClient {
    private var instance: Retrofit? = null

    /*fun getInstance():Retrofit {
        if (instance == null)
            instance = Retrofit.Builder.baseUrl
    }*/
}